#!/bin/bash

# Packs files into archives for github release page

cd `dirname $(readlink -f $0)`

source commits

tar -zcvf CrossOF_Sources.tar.gz                         \
    Archive/$OF_BRANCH.zip                               \
    Dependencies/Archive/glew/$GLEW_BRANCH.zip           \
    Dependencies/Archive/glfw/$GLFW_BRANCH.zip           \
    Dependencies/Archive/poco/$POCO_BRANCH.zip           \
    Dependencies/Archive/freeimage/$FREEIMAGE_BRANCH.zip \
    Dependencies/Archive/fontconfig/$FONTCONFIG_BRANCH.zip

tar -zcvf CrossOF_Binaries.tar.gz        \
    `find Compiled/ -type f -name "*.a"` \
    `find Dependencies/Compiled/ -type f -name "*.a"`
